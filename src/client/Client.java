package client;

import java.io.BufferedInputStream;
import java.io.IOException;
import java.util.Scanner;

public class Client {
	/*
	static ServerLinker baseLink;
	
	static private String host = "localhost";
	
	static private int port = 54433;
	
	static private String userName = null;
	
	static private int roomId = -1;
	
	static Scanner scan = new Scanner(System.in);
	
	static synchronized public String getMessage() {
		return scan.nextLine();
	}
	
	static synchronized public String getName() {
		return scan.nextLine();
	}
	
	static synchronized public int getId() {
		// System.out.println(roomId);
		return scan.nextInt();
	}

	static public void main(String argv[]) {
		baseLink = new ServerLinker(host, port, ServerLinker.BASE);
		baseLink.start();

		while(baseLink.isAlive());
	}
	*/
}
